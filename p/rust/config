upstream="gh:rust-lang/rust"

:release() { :; }

:commit() { :; }

:nightly() {
    upstream="https://static.rust-lang.org/dist/channel-rust-nightly.toml"
    expected="[0-9]{4}-[0-9]{2}-[0-9]{2}"

    fetch() {
        cr | head | grep '^date = ' | cut -d\" -f2
    }
}
