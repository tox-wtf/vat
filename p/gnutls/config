upstream="gh:gnutls/gnutls"

# wouldn't be gnu without a batshit insane tagging scheme
:release() {
    fetch() {
        gr | vtrim -e 's,^gnutls,,' -re 's,(_|-),.,g' | grep -Ev '(win|with|before|real|netconf)' | vfs | vsort
    }
}

:unstable() {
    fetch() {
        gr | vtrim -e 's,^gnutls,,' -re 's,(_|-),.,g' | grep -Ev '(win|with|before|real|netconf)' | vsort
    }
}

:commit() { :; }
