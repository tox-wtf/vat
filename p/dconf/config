upstream="gh:GNOME/dconf"

:release() {

    # TODO: Add a function to filter high version parts
    fetch() {
        gr | vtrim | vsort | grep -Ev '^[0-9]+\.[0-9]+\.9[0-9]+$'
    }
}

:unstable() {
    fetch() {
        gr | vtrim | vsort
    }
}

:commit() { :; }
