upstream="gh:GNOME/dconf"

:release () {
    fetch() {
        defgitrelease | fhigh
    }
}

# What the hell is dconf doing with its tags??
:unstable() {
    expected="([0-9]+\.[0-9]+\.9[0-9]+|[0-9]+\.(beta|rc))"
    fetch() {
        gr | vtrim | vsort
    }
}

:commit  () { :; }
