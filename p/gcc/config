upstream="https://gcc.gnu.org/git/gcc.git"

:release() {
    fetch() {
        gr | vtrim | fsl | fl | grep -F . | vsort
    }
}

:unstable() {
    expected='[0-9]+'

    fetch() {
        gr | vtrim | fsl | vsort
    }
}

:commit() { :; }
